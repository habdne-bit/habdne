TURAB — test run provenance
===========================
This file exists because a saved JUnit report says when IT was produced, not
which tree produced it — and a reviewer rightly declined to read the previous
one as a run at the commit it shipped with. The accurate description of that
artefact was "a saved run on an identical source tree", not "a re-run at this
commit". This binds the two.

Recorded    : 2026-09-23 12:05:46 UTC
Commit      : 44c0a101823c95e4717fc57f392c2931158a9088
Working tree: HEAD was clean when the suite started. The only
              uncommitted paths are the evidence files THIS run
              writes:
                docs/gate/evidence/TEST-RUN-PROVENANCE.txt
                docs/gate/evidence/junit-run.xml
              Neither is in the source fingerprint, so the value
              below is the fingerprint the tests actually ran on.
Source fingerprint (db/dev/source_fingerprint.py):
              6aa7a0567d59b3ec62f4ee9904387acd7f0f93a8b58366d6076498d093eb68f3
Report      : docs/gate/evidence/junit-run.xml
JUnit timestamp / totals:
              timestamp=2026-09-23T12:05:46.732344+00:00
              tests=556 failures=0 errors=0 skipped=0 time=42.475s

WHAT THIS DOES AND DOES NOT SAY
  * It says: these tests were run, at this time, against a tree whose source
    fingerprint is the value above.
  * It does not say the reviewer's environment would produce the same result.
    Nobody but us has re-run this suite; every result we report is ours.
  * The fingerprint covers src/, tests/, db/ and alembic.ini — not docs/. A
    later commit that touches only documentation leaves it unchanged, which is
    why the fingerprint, not the commit id, is what binds a report to the code
    it exercised.
